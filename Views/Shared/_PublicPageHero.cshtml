@model croupe_06_TournoiGolf.Models.ViewModels.PublicPageHeroViewModel

<section class="public-section-card public-hero-strip">
    <span class="public-eyebrow"><i class="@Model.EyebrowIcon"></i> @Model.EyebrowText</span>
    <h1 class="public-heading">@Model.Title</h1>
    <p class="public-lead">@Model.Description</p>
</section>
